export type InstanceInfoType = {
    name: string,
    type: string,
    port: number,
    metricsPort: number,
    commandsPort: number,
    processId: number,
    exitCode: string,
    isRunning: string
}